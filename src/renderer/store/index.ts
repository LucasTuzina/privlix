// Pinia store configuration
import { createPinia } from 'pinia'

// Create the main store instance
export const pinia = createPinia()

// You can add store plugins here if needed
// For example:
// pinia.use(somePlugin)

export default pinia
